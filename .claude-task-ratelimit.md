# Task: Rate limiting for /v1 + body validation hardening

Lavora su /home/serverino/Serverino/agent-first-crm (workdir attivo).

## CONTESTO PROGETTO
Questo repo e un clone opensource API-compatibile con CRM diffusi (naming generico, mai il nome del CRM di origine). Surface API su /v1 in src/routes/v1.js con middleware tenancy/auth in src/middleware/tenant-api.js (requireTenant: header Location-Id + Bearer API key, header Version ignorato). DB PostgreSQL via pool in src/db.js. Test con node:test + express server su porta 0. Test DB: container Docker cms-test-pg (localhost:15999, db testdb). Script test: ./scripts/test.sh <file>. express-rate-limit ^7.4.1 gia installato.

## COSA COSTRUIRE
Rate limiting per la surface /v1 + body validation hardening (nessuna migrazione DB nuova).

### TASK 1
Crea file src/middleware/rate-limit-v1.js con:
- Un middleware factory `v1RateLimiter()` che restituisce un rate limiter express.
- Due rate limiters interni:
  - generalLimiter: 120 req/min per GET
  - writeLimiter: 60 req/min per POST/PUT/DELETE (windowMs: 60*1000)
- Skip per loopback (127.0.0.1, ::1, ::ffff:127.0.0.1).
- standardHeaders: true, legacyHeaders: false.
- message: { error: "Troppe richieste. Riprova piu tardi." }
- Esporta: export function v1RateLimiter() { ... return (req, res, next) => { ... }; }
- Aggiungi commento in testa file: "Rate limiting per la surface /v1 API-compatibile. Configurabile per tenant via tenant_config key=rate_limits come { general: { windowMs, max }, write: { windowMs, max } }."

### TASK 2
Modifica src/routes/v1.js:
- Importa v1RateLimiter da "../middleware/rate-limit-v1.js"
- Aggiungi DOPO router.use(requireTenant()) ma PRIMA di tutte le route:
  router.use(v1RateLimiter());
- NON toccare le route esistenti.

### TASK 3
Crea file src/middleware/body-validate-v1.js con:
- Un middleware factory `v1BodyValidator()`.
- Per richieste POST/PUT/DELETE con body: se Content-Type non e application/json -> 415 { error: "Content-Type deve essere application/json" }.
- Per POST: se req.body e undefined/vuoto -> 400 { error: "Body JSON richiesto" }.
- Skip per GET e DELETE (non hanno body significativo).
- Esporta: export function v1BodyValidator() { ... return (req, res, next) => { ... }; }

### TASK 4
Modifica src/routes/v1.js per montare anche v1BodyValidator DOPO v1RateLimiter ma PRIMA delle route:
- Importa v1BodyValidator da "../middleware/body-validate-v1.js"
- router.use(v1BodyValidator());
- NON toccare le route esistenti.

### TASK 5
Crea test/v1-rate-limit.test.js:
- Pattern identico a test/f0-foundations.test.js (usa createTestSite, crea API key, express server su porta 0).
- Test 1: GET /v1/contacts senza rate limit -> 200.
- Test 2: rate limit GET (crea un server separato con limiter windowMs=1000, max=5, usa express-rate-limit direttamente con rateLimit({...}) come fa test/auth-rate-limit.test.js) - manda 6 richieste veloci, verifica che la sesta sia 429.
- Test 3: POST /v1/custom-fields con body vuoto -> 400 da body validation.
- Test 4: POST /v1/custom-fields con Content-Type text/plain -> 415.
- Test 5: rate limit WRITE (POST) con limiter separato (max=3 per write) - manda 4 POST, verifica che la quarta sia 429.
- Test 6: loopback esenzione (usa skip function che esenta req.ip === '127.0.0.1').

### TASK 6
- node --check su TUTTI i file creati/modificati.
- Esegui: ./scripts/test.sh test/v1-rate-limit.test.js
- Esegui: ./scripts/test.sh test/f0-foundations.test.js per regressione.
- Nessun segreto/personale nel codice.
- Nessun nome del CRM di origine.
- COMMIT: git add -A && git -c user.name="MioArubaCode" -c user.email="dev@local" commit -m "feat: rate limiting + body validation per /v1 surface"
- Niente git push (nessun remote).

ESEGUI TUTTO ADESSO. Non fermarti dopo la scrittura codice. Lancia test, verifica, committa. Riporta esito finale.